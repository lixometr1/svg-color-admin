{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "import.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/upload",
      "handler": "import.upload",
      "config": {
        "policies": []
      }
    }
  ]
}
